var express = require('express');
var router = express.Router();
var table = rootRequire('common/table');
var responder = rootRequire('common/responder');

var authModel = rootRequire('models/auth');




module.exports = router;
